[project]
name = "my-products"
version = "0.1.0"
description = ""
requires-python = ">=3.11"
dependencies = [
    "mkdocs",
    "pymdown-extensions",
    "mkdocs-material",
    "mkdocs-encryptcontent-plugin",
    "jinja2",
    "mkdocs-link-embeds-plugin",
]

[tool.uv]
dev-dependencies = []
